# Production build — set this in the Vercel project's Environment Variables.
# Point it at the deployed FastAPI backend, including the /api prefix.
# Example:
#   VITE_API_BASE=https://flux-api.fly.dev/api
VITE_API_BASE=https://YOUR-BACKEND-HOST/api
